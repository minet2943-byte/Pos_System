function PagePlaceholder({ title }) {
  return (
    <section className="content-panel">
      <h2>{title}</h2>
      <p className="muted">This module is ready for forms, tables, filters, and API integration.</p>
    </section>
  )
}

export default PagePlaceholder
